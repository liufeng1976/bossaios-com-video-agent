# BossAI Video Agent Windows inference dependency lock for pinned CosyVoice source revision
# 074ca6dc9e80a2f424f1f74b48bdd7d3fea531cc.
#
# Scope: customer-side CosyVoice2 inference only.
# Excluded intentionally: training, Gradio/FastAPI demos, TensorBoard, DeepSpeed,
# TensorRT, ModelScope/WeText runtime downloading, PyWorld dataset processing,
# and optional Matcha training/audio-dataset helpers.
#
# PyTorch/TorchAudio are installed separately from SHA-256-pinned official wheels.
# huggingface_hub is installed separately by the installer and is used only for
# installer-time pinned model retrieval, never for inference-time fallback downloads.

# Matcha-TTS note: matcha/utils/__init__.py eagerly imports its training and
# reporting helpers, and cosyvoice.flow.flow_matching imports matcha.utils.
# pylogger, so importing the inference path executes that __init__. hydra-core,
# lightning, matplotlib, rich, gdown and wget are therefore inference
# requirements here despite being training-side dependencies upstream; without
# them CosyVoice2 inference fails at import with ModuleNotFoundError. Versions
# match the upstream pinned revision's requirements.txt.

conformer==0.3.2
diffusers==0.29.0
gdown==5.1.0
hydra-core==1.3.2
HyperPyYAML==1.2.3
inflect==7.3.1
librosa==0.10.2
lightning==2.2.4
matplotlib==3.7.5
numpy==1.26.4
omegaconf==2.3.0
onnxruntime==1.18.0
openai-whisper==20231117
protobuf==4.25
pyarrow==18.1.0
pyworld==0.3.4
rich==13.7.1
soundfile==0.12.1
transformers==4.51.3
wget==3.2
x-transformers==2.11.24
