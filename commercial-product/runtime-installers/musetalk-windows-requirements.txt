# BossAI Video Agent MuseTalk v1.5 Windows inference runtime.
# Derived from the pinned upstream MuseTalk requirements and the actual
# scripts.inference import graph used by commercial-product/backend/musetalk_adapter.py.
# Training/demo-only dependencies (TensorFlow, TensorBoard, Gradio, gdown,
# MoviePy, imageio UI helpers) are intentionally excluded from customer runtime.

diffusers==0.30.2
accelerate==0.28.0
numpy==1.23.5
opencv-python==4.9.0.80
soundfile==0.12.1
transformers==4.39.2
huggingface_hub==0.30.2
librosa==0.11.0
einops==0.8.1
omegaconf==2.3.0
tqdm==4.67.1
